// src/api/health/get/route.js -> GET /health   (public — see PUBLIC_PATHS in app.js)
// Folder-per-method: the `get` folder makes this a GET; `route.js` default-exports the handler.
// No schema.json here (nothing to validate). Pings the DB via the data layer to prove
// connectivity, and uses the response envelope helper `res.ok`.

import { query } from '#data/pool.js';

export default async (req, res) => {
  await query('select 1');
  res.ok({ status: 'ok' });
};
