# Changelog

## 1.0.0
- Initial release: `postgres-setup`, `postgres-up`, `postgres-down`, `postgres-status`,
  `postgres-logs`, `postgres-reset`, `postgres-query`, `postgres-backup` — a project-local,
  self-contained Dockerized Postgres, adapted from the `dev-stack` repo's proven compose and
  atomic-skill conventions.
