# Changelog

## 1.0.0
- Initial release: `kafka-setup`, `kafka-up`, `kafka-down`, `kafka-status`, `kafka-logs`,
  `kafka-reset`, `kafka-topics` — a project-local, self-contained Dockerized single-node KRaft
  Kafka (with optional kafka-ui), adapted from the `dev-stack` repo's proven compose and
  atomic-skill conventions, including its dual-listener pattern.
