namespace {{Sln}}.{{Name}}.Application;

// Public marker type so Infrastructure's Add{{Name}}Module can scan this assembly — by reference,
// without naming any internal type — for command/query handlers (Scrutor), FluentValidation
// validators, and IEndpoint implementations, all of which live here.
public sealed class ApplicationAssemblyMarker;
